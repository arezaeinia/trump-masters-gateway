# Local/Load-test profile configuration
# Activates with: --spring.profiles.active=local
# Used for load testing with WireMock (no authentication required)
server:
  port: 9090

service:
  token:
    enabled: false
